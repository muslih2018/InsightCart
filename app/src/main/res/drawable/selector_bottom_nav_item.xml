<!-- selector_bottom_nav_item.xml -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Item in selected state -->
    <item android:color="@color/ungu" android:state_checked="true" />
    <!-- Item in default state -->
    <item android:color="@color/menu_icon_color" />
</selector>
